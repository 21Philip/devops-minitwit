MINITWIT_DB_CONNECTION=Host=${postgres_host};Database=${postgres_db};Username=${postgres_user};Password=${postgres_password};Port=5433
GRAFANA_DB_CONNECTION=postgres://${postgres_user}:${postgres_password}@${postgres_host}:5434/${postgres_db}
